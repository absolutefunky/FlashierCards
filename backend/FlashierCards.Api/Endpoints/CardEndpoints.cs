using FlashierCards.Api.Dtos.CreateDtos;
using FlashierCards.Api.Dtos.ReturnDtos;
using FlashierCards.Api.Dtos.UpdateDtos;
using FlashierCards.Api.Models;

namespace FlashierCards.Api.Endpoints;

public static class CardEndpoints
{
    public static void MapDeckEndpoints(this WebApplication app)
    {
        // GET /users/{userId}/decks{id} to return a document (deck of cards)

        // POST /users/{userId}/decks/{id}/create to create a document (deck of cards)

        // PUT /users/{userId}/decks/{id}/update to update a document (deck of cards)

        // DELETE  /users/{userId}/decks/{id}/delete to delete a document (deck of cards)
    }
}
